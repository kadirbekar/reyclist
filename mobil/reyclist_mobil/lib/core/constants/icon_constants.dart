import 'package:flutter/material.dart';

class IconConstants {
  static Icon email = const Icon(Icons.email);
  static Icon lock = const Icon(Icons.lock);
  static Icon person = const Icon(Icons.person);
  static Icon phone = const Icon(Icons.phone);
}
